# ASSETS / Sprite Bible (TIC-80)

Документ фиксирует требования к спрайтам и их адресации (id), чтобы рисование “руками” не приводило к переделкам кода.

## Общие правила

- **Сетка**: базовая — `8×8` (TIC-80). Объекты могут занимать блок `16×16` как 2×2 тайла.
- **Прозрачность**: **цвет 0** = прозрачный (`colorkey = 0`).
- **Позиционирование объектов в мире**: “точка на земле” = **центр по X + низ по Y**.
- **Пивоты** (если объект рисуется слоями): одинаковая “точка крепления” во всех кадрах.
- **Стиль**: крупные читаемые формы, реалистичнее за счёт света/теней, а не мелких деталей.
- **Источник света**: один для всех ассетов (рекомендуем: сверху-слева).

### Важное про id и размеры в TIC-80 (чтобы правильно рисовать 16×16)

- В TIC-80 базовый спрайт — **8×8** и имеет **id**.
- Объект **16×16** занимает **4** спрайта 8×8 (2×2 блок).
- Если “база” (верхний-левый) спрайта 16×16 имеет id = `S`, то остальные тайлы:
  - верхний-правый: `S+1`
  - нижний-левый: `S+16`
  - нижний-правый: `S+17`

В этом документе, когда указано `16×16`, **id означает верхний-левый (base id)**.

Практическое следствие (ВАЖНО для твоего спрайт-листа):
- если заняли `16×16` с base id = `S`, то **нельзя** использовать `S+1`, `S+16`, `S+17` под отдельные `8×8` спрайты — они уже заняты этим 16×16 блоком.

## Танк (base id зарезервированы) — ПОДТВЕРЖДЕНО

### Слои

- **Корпус**: `16×16`
- **Башня**: `16×16` (отдельно)
- **Ствол**: `16×16` (отдельно)

### Пивоты (в координатах спрайта 16×16)

- **Точка танка на земле** (в спрайте корпуса): `(8,15)`
- **Пивот башни**: `(8,9)`
- **Пивот ствола**: `(8,9)` (крепление к башне)

Требование: во всех кадрах углов **пивот остаётся на месте**, меняется только геометрия вокруг него.

### Углы поворота (кадры)

TIC-80 не поддерживает произвольный угол поворота `spr()`, поэтому поворот делаем кадрово.

- **7 углов**: `-30, -20, -10, 0, +10, +20, +30`

### Таблица спрайтов (id)

- `0` — `tank_body_0` (гусеницы кадр A) `16×16`
- `2` — `tank_body_1` (гусеницы кадр B) `16×16`
- `4` — `tank_turret_a0` `16×16`
- `6` — `tank_turret_a1` `16×16`
- `8` — `tank_turret_a2` `16×16`
- `10` — `tank_turret_a3` `16×16`
- `12` — `tank_turret_a4` `16×16`
- `14` — `tank_turret_a5` `16×16`
- `32` — `tank_turret_a6` `16×16`
- `34` — `tank_barrel_a0` `16×16`
- `36` — `tank_barrel_a1` `16×16`
- `38` — `tank_barrel_a2` `16×16`
- `40` — `tank_barrel_a3` `16×16`
- `42` — `tank_barrel_a4` `16×16`
- `44` — `tank_barrel_a5` `16×16`
- `46` — `tank_barrel_a6` `16×16`

## Препятствия (предлагаемый пул на MVP)

Все препятствия рисуются как `16×16`, “точка на земле” — центр-низ.

### Диапазоны id (ПРИНЯТО)

- `0..63` — танк (занимает первые 4 ряда спрайтов 8×8, включая “соседние” тайлы 16×16)
- `64..127` — препятствия / окружение (base id для `16×16`)
- `128..191` — FX (`8×8`)
- `192..255` — UI (позже)

### Твоя текущая карта занятости (УТВЕРЖДЕНО)

- **Свободный диапазон начинается с `152`** (до `191` включительно — свободно).
- **`192` занято под `ui_ammo`**, и **`193+` тоже свободно**.
- Следствие для будущих ассетов:
  - новые `16×16` объекты по умолчанию размещаем начиная с `152` (и учитываем, что 16×16 занимает также `S+1/S+16/S+17`)
  - новые FX `8×8` после `132` можно спокойно класть в `133+` (как мы уже начали)

### Базовый набор (общий)

- **Дерево**: неразрушаемое
- **Камень**: неразрушаемое
- **Стена**: разрушаемая (минимум 2 состояния: целая / разрушенная; опционально “потрескан”)

### Биомы (утверждено)

- **Лес**: дерево, камень, стена/баррикада (разрушаемая)
- **Пустыня**: кактус/куст, камень, стена/руина (разрушаемая)
- **Город**: руины/баррикады + городские поверхности (асфальт/переход/лужи). **Физики скольжения нет**.

### Доп. требования к разрушаемым

- У разрушаемого объекта должны быть:
  - визуальная стадия “целый”
  - визуальная стадия “разрушен”
  - (опционально) промежуточная стадия “потрескан”

### Правило урона (УТВЕРЖДЕНО)

- **Все разрушаемые препятствия разрушаются за 1 попадание**.
  - следствие: стадия `*_cracked` остаётся опциональной и используется только если захотим визуально “предупредить” игрока, но на HP не влияет

### Таблица препятствий (id → имя → размер) — MVP

Лес:
- `64` — `obs_tree` `16×16`
- `66` — `obs_rock` `16×16`
- `68` — `obs_wall_intact` `16×16`
- `70` — `obs_wall_cracked` `16×16` (опционально, но лучше нарисовать сразу)
- `72` — `obs_wall_broken` `16×16`

Пустыня:
- `74` — `obs_cactus` `16×16`
- `76` — `obs_bush` `16×16` (можно использовать как “мелкое” препятствие)
- `78` — `obs_ruin_intact` `16×16` (разрушаемая стена/руина)
- `96` — `obs_ruin_broken` `16×16` *(перенесено: `80` занято в твоём спрайт-листе)*

Город (поверхности):
- `152` — `tile_city_asphalt` `16×16` (асфальт, базовая поверхность)
- `154` — `tile_city_crosswalk` `16×16` (пешеходный переход / разметка)
- `156` — `tile_city_puddle` `16×16` (лужа/мокрый асфальт)

Город (препятствия):
- `158` — `obs_city_car` `16×16` (твёрдое препятствие, в текущем геймплее помечено как `X/O` → **смертельно**)
- `178` — `obs_city_dumpster` `16×16` (твёрдое препятствие, в текущем геймплее помечено как `X/O` → **смертельно**)

### Правила поверхностей (город)

- Поверхности `tile_city_*` — **визуальные** (укладываются под лабиринтом) и **не меняют физику**.
- Если захотим позже — можно добавить “опасные” поверхности (масло/ямы), но **скольжение сейчас убрали полностью**.

### Зоны поверхностей (город)

- Пока поверхности — чисто декоративные, поэтому нет требований “объезда зон”.

### Проезжая область по ширине — УТВЕРЖДЕНО

- **Весь экран доступен как поле** (нет “дороги” фиксированной ширины).
- Следствие для генератора:
  - любые “большие зоны” (вода/лёд) должны оставлять свободный обход **по краю слева или справа**
  - непреодолимые препятствия не должны формировать “стену” от края до края

## Эффекты (FX) — требования к минимуму

- **Снаряд/пуля**: `8×8` (1 спрайт)
- **Вспышка выстрела**: `8×8` (2 кадра)
- **Пыль из-под гусениц**: `8×8` (2 кадра)
- **Осколки/искры от стены**: `8×8` (2 кадра)

### Таблица FX (id → имя → размер) — MVP

- `128` — `fx_bullet` `8×8`
- `129..130` — `fx_muzzle_0..1` `8×8`
- `131..132` — `fx_dust_0..1` `8×8`
- `98` — `fx_splash_0` `8×8` *(свободный слот в твоём листе)*
- `100` — `fx_splash_1` `8×8` *(свободный слот в твоём листе)*
- `102` — `fx_ice_shards_0` `8×8` *(свободный слот в твоём листе)*
- `108` — `fx_ice_shards_1` `8×8` *(свободный слот в твоём листе)*
- `133..134` — `fx_debris_0..1` `8×8` *(после 132 у тебя свободно)*
  - `110` — резерв под доп. FX, если понадобится

## Стрельба и боезапас — геймплейные требования (УТВЕРЖДЕНО)

- **Боезапас ограничен**: у игрока есть счётчик патронов.
- **Перезарядка присутствует**:
  - стрельба идёт очередью/выстрелами до опустошения “магазина”
  - когда магазин пуст — происходит перезарядка, во время которой стрелять нельзя
- **Подбор боеприпасов на карте**:
  - есть объект-подбор `item_ammo_box`
  - при столкновении с танком добавляет патроны и исчезает

### Параметры для MVP (можно тюнить, но фиксируем форму механики)

- `ammo_total_start = 20`
- `ammo_mag_size = 6`
- `ammo_reload_frames = 60` (≈ 1 сек при 60 FPS)
- `ammo_pickup_amount = 10`

### Спрайты для боезапаса (нужно нарисовать)

Мир (подбор):
- `150` — `item_ammo_box` `16×16` *(по твоей раскладке: после 150 свободно)*

UI:
- `192` — `ui_ammo` `8×8` *(чтобы не пересекаться с 16×16 блоками рядом с `150`)*

## Биомы (локации)

## Столкновения — геймплейные требования (УТВЕРЖДЕНО)

- **Твёрдые препятствия** (дерево, камень, ледяной камень, любые стены в состоянии `*_intact`):
  - при столкновении **препятствие остаётся**
  - танк **моментально останавливается по скорости “вперёд”** (без отбрасывания)
  - боковая инерция/скольжение по X **не обнуляется автоматически**
- **Вода** (`tile_water`): непроходимо + моментальная остановка (как твёрдая граница)

Требование: 3 набора окружения и цветового настроения.

- **Лес**:
  - основные препятствия: `obs_tree`, `obs_rock`, `obs_wall_*`
  - FX: `fx_dust_*` (+ опционально листья позже)
- **Пустыня**:
  - основные препятствия: `obs_cactus`, `obs_bush`, `obs_ruin_*`, `obs_rock`
  - FX: `fx_dust_*` (песчаная пыль — цветом/палитрой)
- **Зима**:
  - основные препятствия/поверхности: `tile_water`, `tile_ice`, `obs_ice_rock`, `obs_icewall_*`
  - FX: `fx_splash_*`, `fx_ice_shards_*`

## Следующий документ (когда будем готовы)

Когда утвердим точные наборы препятствий, заведём расширенную таблицу:

- id → имя → размер → якорь → коллизия (hitbox) → разрушаемость → количество кадров

